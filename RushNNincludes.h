#include <stdio.h>
#include <stdlib.h>
#include <math.h>

struct gameData
{
	double a;
	double b;
	double c;
	double d;
	double e;
	double f;
	double g;
	double csharp;
	double rush;
	int u[12];
};

char nn_run(struct gameData, float*, float*, int*, int*);
